## No Capability Changes

This is a pure refactoring change. No new capabilities are introduced and no existing capability requirements are modified. All changes are implementation-level only — the public behavior of every module remains identical.

See `proposal.md` for the full list of refactoring targets and `design.md` for technical decisions.
