/// The fundamental symbols in Morse code.
enum MorseSymbol {
  /// A short signal (·).
  dot,

  /// A long signal (−).
  dash,

  /// Inter-character gap within a multi-character word.
  charGap,
}
